package model;

public class Artist extends Producer {

    public Artist (String name, String date, String url, int playBack, int playTime){
        super(name, date, url, playBack, playTime);
       
    }

}